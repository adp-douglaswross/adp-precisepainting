# Issues Log

## Open

1. **Seed object types test baseline mismatch (resolved in this run)**  
   Added `template` key alias in `src/eai.config/object-types.ts` to satisfy existing seed test assumptions.

2. **Playwright screenshot capture environment dependency**  
   Browser screenshots require host packages; functional route checks and unit tests are used as primary evidence in this environment.
